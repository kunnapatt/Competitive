#include <bits/stdc++.h>

using namespace std ;

int main() {
    bool check = false ;
    bool b = false ;

    if ( check == false ) {
        cout << "A" ;
        b = true ;
    }
    if ( b == true ) {
        cout << "B" ;
    }
    return 0 ;
}
