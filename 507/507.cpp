#include <bits/stdc++.h>

using namespace std ;

#define FOR(i, a, b) for(int i = a ; i < b ; i++)

int main(){
    int n ;
    cin >> n ;
    FOR(i, 0, n){
        int m ;
        cin >> m ;
        FOR(j, 0, m-1){
            int in ;
            cin >> in ;
        }
    }
    return 0 ;
}
