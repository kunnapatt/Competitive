#include <bits/stdc++.h>

using namespace std ;

#define FOR(i, a, b) for(int i = a ; i < b ; i++)
#define vi vector<int>
#define long long long

int main() {
    int n, m ;
    //freopen("../test.in", "r", stdin) ;
    while ( cin >> n >> m ){
        cout << n*m*2 << endl ;
    }

    return 0 ;
}

